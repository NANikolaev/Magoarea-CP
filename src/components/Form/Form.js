import { useParams } from "react-router-dom";

import { Section } from "./elements";

const Form = () => {
    let id = useParams().id

    return (
      <Section>
        
      </Section>
       
    )

};

export default Form